#include <16F628A.h>

#FUSES NOWDT                    //No Watch Dog Timer
#FUSES NOBROWNOUT               //No brownout reset
#FUSES NOLVP                    //No low voltage prgming, B3(PIC16) or B5(PIC18) used for I/O

#use delay(crystal=20000000)

#define LED PIN_None
#define DELAY 1000







void main(){
   while(true){
      
      /*
      if(input(pin_a1)==0){
         output_high(pin_a0);
      }
      if(input(pin_a1)==1){
         output_low(pin_a0);
      }
      */
      output_low(pin_a0);
      if(input(pin_a1)==0){
         output_high(pin_a0);
      }
      
    }
   }






/*

while(true){
sempre verde para carro

botao = entrada de sinal(true)
if(botao = true){
   funcao display7seg(20);
}

}



*/
