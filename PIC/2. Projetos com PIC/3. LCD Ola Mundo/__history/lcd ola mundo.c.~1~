#include <lcd ola mundo.h>

#include <lcd.c>

void main()
{
	char k;


   lcd_init();

	lcd_putc("\fReady...\n");

	while(TRUE)
	{
		k = kbd_getc();
		if(k!=0)
			if(k=='*')
				lcd_putc('\f');
			else
				lcd_putc(k);

		//TODO: User Code
	}

}
